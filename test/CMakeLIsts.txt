set(TEST_SOURCES
        main.cpp
        basic/basic_sum_test.cpp
        )

set(TEST_LIBS
        basic_lib
        )

add_executable(run_tests ${TEST_SOURCES})
target_link_libraries(run_tests ${TEST_LIBS} ${CONAN_LIBS})
target_include_directories(run_tests PRIVATE ${CMAKE_SOURCE_DIR}/src)

add_test(NAME array_tests WORKING_DIRECTORY ${CMAKE_BINARY_DIR}/bin COMMAND array_tests)
